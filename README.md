# 🧠 Générateur IA de CV + Lettre de motivation

Une application Streamlit qui génère automatiquement un CV structuré et une lettre de motivation en français, exportables en PDF avec une mise en page élégante.

---

## 🚀 Installation

Clonez le projet ou décompressez le dossier `cv_ia`.

Installez les dépendances sur le terminal :

```bash
pip install -r requirements.txt
